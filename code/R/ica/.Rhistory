dim(dtm)
dim(dtm1)
dim(dtm2)
loc = "/home/carmen/Dropbox/Thesis/Data/text/SpecializedSet/"
loc
(docCorpus <- Corpus(DirSource(loc),readerControl = list(language = "en")))
library(tm)
(docCorpus <- Corpus(DirSource(loc),readerControl = list(language = "en")))
textCorpus <- docCorpus
textCorpus  <- tm_map(textCorpus, tolower)
    textCorpus <- tm_map(textCorpus,removePunctuation)
    textCorpus <- tm_map(textCorpus,removeNumbers)
docTermM <- DocumentTermMatrix(textCorpus)
dtm <- docTermM
save dtm
save(dtm)
save
save.image()
spdtm <- removeSparseTerms(dtm,0.1)
dtmMat <- as.matrix(spdtm)
featureMeans <- colMeans(dtmMat)
 newVectors <- t(apply(dtmMat, 1, "-", c(featureMeans)))
comb <- list(newVectors,featureMeans)
nV <- comb[1]
m <- comb[2]
m
nV[1:10,1:10]
nV[1,1]
nV[1,]
nV
los <- rmvmean(spdtm)
source("rmvMean.R")
los <- rmvMean(spdtm)
v <- los[1]
v
save.image()
q()
los
nV
cov <- cov(nV)
wordMatrix = as.data.frame( t(as.matrix(nV ))) 
cov <- cov(wordMatrix)
wordMatrix = as.data.frame(nV ) 
cov <- cov(wordMatrix)
cov
cov[1:10,1:20]
wordMatrix = as.data.frame(t(nV)) 
cov <- cov(wordMatrix)
wordMatrix[1:20,1:10]
wordMatrix[1:10,1:10]
wordMatrix = as.data.frame(nV) 
cov <- cov(wordMatrix)
wordMatrix[1:10,1:10]
wordMatrix[1:10,1:20]
cov[1:10,1:20]
cov = cov(wordMatrix)
cov[1:10,1:20]
eigens <- eigen(cov)
evals <- eigens$values
evecs <- eigens$vectors
evals
whitMat = inv (sqrt (evals)) * t(evecs)
install.packages(c("riv"))
whitMat = inv (sqrt (evals)) * t(evecs)
library(riv)
whitMat = inv (sqrt (evals)) * t(evecs)
whitMat = inv(sqrt (evals)) * t(evecs)
whitMat <- solve(sqrt (evals)) * t(evecs)
evals <- eigens$values
evals1 <-sqrt(eigens$values)
eigens$values
evals1
evals1 <-sqrt(as.complex(eigens$values))
evals
whitMat <- solve(evals1) * t(evecs)
evals
whitMat <- solve(evals1)
whitMat <- qr.solve(evals1)
det(evals1)
evals1 <- sqrt(eigens$values)
install.packages(c(pixmap'))
install.packages(c("pixmap"))
library(pixmap)
evals1 <- sqrt(eigens$values)
install.packages(c("MASS"))
evals1 <- sqrt(as.complex(eigens$values))
library(MASS)
e <- ginv(evals1)
e
whitMat <- e * t(evecs)
dim(e)
dim(evecs)
whitMat <- e * evecs
whitMat <- as.vector(e) *t(evecs)
whitMat
dim(xwhit)
dim(whitMat)
dim(spDtm)
dim(spdtm)
spdtm <- removeSparseTerms(dtm,0.1)
library(tm)
spdtm <- removeSparseTerms(dtm,0.1)
dim(spdtm)
dim(whitMat)
nVec <- whitMat * spdtm
nVec <- whitMat * wordMat
wordMatrix = as.matrix(spdtm)
nVec <- whitMat * wordMatrix
nVec <- whitMat%*% wordMatrix
nVec <- apply(whitMat,2,"*", wordMatrix)
nVec
dim(nVec)
q()
